#include <stdio.h>
int main(void){

    //A program requesting a person to enter a number and print the output between the two text marks "

    int n;
    printf("enter your numper: ");
    scanf("%d" , &n);

    printf("\"%d\"" , n);

    return 0;
}